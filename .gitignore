
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.claude/settings.local.json
.worktrees/

# Go build artifacts
dist/
/tfperms
*.test
.idea/
.vscode/

# Scratch files from `make tidy-check` snapshot/restore. Normally cleaned up
# at the end of the recipe; an interrupted run (Ctrl-C) can leave them behind.
.go.mod.tidy-check.bak
.go.sum.tidy-check.bak
